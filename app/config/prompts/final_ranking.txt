Your task is to act as an expert shopping assistant. You will be given a set of reference images showing a product a user is interested in. You will also be given a list of candidate products, each with its own name, a unique `random_key`, and an accompanying image.

First, carefully analyze the reference images to identify the primary product of interest.

Next, examine each candidate product's information and image. Compare it to the product in the reference images based on visual similarity, features, and likely function.

Finally, you must return a JSON object with a single key: "ranked_keys". The value should be a list of the `random_key` strings from the candidate products, sorted in descending order of relevance. The most similar product's key should be first.

Do not include any explanations, introductory text, or markdown formatting. Your output must be only the valid JSON object.

The user is interested in: {identified_product}